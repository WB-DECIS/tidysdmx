# Keeps non-ruff editors agreeing with ruff about whitespace.
# https://editorconfig.org
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 4

[*.{yml,yaml,json,qmd,md}]
indent_size = 2

# pyproject.toml is written with 4-space arrays, the same as the Python it
# configures.
[*.toml]
indent_size = 4

[*.md]
# Two trailing spaces are a hard line break in Markdown.
trim_trailing_whitespace = false

[Makefile]
indent_style = tab
